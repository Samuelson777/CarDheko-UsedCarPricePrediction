streamlit==1.12.0      # or the latest compatible version
pandas==1.4.2           # or the latest compatible version
numpy==1.22.0           # or another compatible version
scikit-learn==1.0.2     # or the latest compatible version
Pillow==9.1.0           # For image processing (from PIL)
pickle5==0.0.11         # If you need pickle compatibility (optional)
streamlit==1.12.0  # Specify the version of Streamlit you're using
matplotlib==3.5.1    # For plotting (if you use any plots)
seaborn==0.11.2     # For enhanced visualizations (if applicable)
joblib==1.1.0       # For saving and loading models (if applicable)
python -m pip install --upgrade pip
